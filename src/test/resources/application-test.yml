spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  profiles:
    active: local
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    database: h2
    hibernate:
      ddl-auto: update
    open-in-view: false
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql # 더미데이터 파일 연결
      schema-locations: classpath:schema.sql # 스케마 파일 연결